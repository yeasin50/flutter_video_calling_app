name: web_rtc
description: Flutter web rtc project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  easy_rich_text: ^2.2.0
  flutter_webrtc: ^0.11.6+hotfix.1
  google_fonts: ^6.2.1
  intl: ^0.20.0
  permission_handler: ^11.3.1
  provider: ^6.1.2
  sdp_transform: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
